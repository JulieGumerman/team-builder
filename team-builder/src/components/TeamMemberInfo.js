import React from "react";
import styled from "styled-components";

const TeacherCard = styled.div`
    width: 400px;
    border: 1px solid black;
`

const TeamMemberInfo = props => {

    
    return (
        <div>
            {props.teachers.map(person => <h1>What?????</h1>)}
        </div>
    )
}

